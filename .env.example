# ═══════════════════════════════════════════════════════════════════════════════
# Cursor Orchestrator – Self-Hosted Configuration
# Cost: $0/month (runs on your own PCs with Cloudflare Tunnel)
# ═══════════════════════════════════════════════════════════════════════════════

# ── Gemini API (intent parsing + voice transcription) ─────────────────────────
GEMINI_API_KEY=your-gemini-api-key
GEMINI_MODEL=gemini-2.5-flash

# ── Telegram Bot (optional) ──────────────────────────────────────────────────
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_ALLOWED_USER_IDS=123456789

# ── WhatsApp Business Cloud API (optional) ───────────────────────────────────
WHATSAPP_TOKEN=your-whatsapp-permanent-token
WHATSAPP_PHONE_NUMBER_ID=your-phone-number-id
WHATSAPP_VERIFY_TOKEN=your-webhook-verify-token
WHATSAPP_ALLOWED_NUMBERS=34612345678

# ── Cursor Background Agent API (Opus 4.6 Max – company paid) ────────────────
CURSOR_API_KEY=your-cursor-api-key

# ── Claude API (optional) ────────────────────────────────────────────────────
ANTHROPIC_API_KEY=

# ── Home Assistant (domotica) ─────────────────────────────────────────────────
# URL of your HA instance (docker compose up → http://localhost:8123)
HA_URL=http://localhost:8123
# Long-lived access token: HA dashboard → Profile → Security → Long-Lived Access Tokens
HA_TOKEN=your-ha-long-lived-access-token

# ── Agent Mesh (multi-PC) ────────────────────────────────────────────────────
WS_SECRET=your-strong-shared-secret

# ── Agent daemon config (set on EACH PC that runs the daemon) ─────────────────
# WS_URL=ws://your-main-pc:8000/ws/agent    # URL of the orchestrator
# AGENT_NAME=my-macbook                      # friendly name for this PC
# AGENT_MAX_TASKS=3                          # max concurrent tasks
# PROJECTS_DIR=/Users/you/dev                # auto-discover projects here

# ── Server ───────────────────────────────────────────────────────────────────
HOST=0.0.0.0
PORT=8000
WEBHOOK_URL=https://your-tunnel.cfargotunnel.com    # Cloudflare Tunnel URL
LOG_LEVEL=INFO

# ── Database ─────────────────────────────────────────────────────────────────
DATABASE_PATH=data/orchestrator.db
